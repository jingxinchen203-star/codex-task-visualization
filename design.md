# Projectboard 设计语言

Projectboard 的界面目标不是“展示很多状态”，而是让用户在几秒内感到：事情是清楚的、可推进的，而且工作台正在替自己守住上下文。

当前视觉方向名为 **Obsidian Silver / 黑曜银**。它为 Codex 的暗色宿主而设计：大面积使用中性炭黑、石墨灰和柔和银白，不使用绿色、蓝黑科技色或粉紫色；银灰表达整体交互，暖象牙色表达正在推进，少量琥珀色表达需要用户接棒。目标是让宿主与看板自然衔接，同时将彩色刺激降到最低，获得稳定、耐看的长时间工作体验。

## 设计来源与方法

本设计吸收了 [JimLiu/baoyu-design](https://github.com/JimLiu/baoyu-design) 的以下方法，但不把该 Skill 或任何远程运行时代码加入产品：

- **真实上下文优先**：先阅读现有 HTML、CSS、数据模型和安全边界，再做视觉决策；生产代码是设计事实，截图只用于校验。
- **先建立系统，再画组件**：先确定字体、色彩、空间、状态语义和交互反馈，避免每张卡片各自发挥。
- **选择一个明确方向**：本项目只执行“黑曜银”，不混合玻璃拟态、霓虹、蓝黑科技风和多套互相竞争的风格。
- **信息必须有用**：不添加装饰性统计卡、占位数据或为了填满页面而出现的元素。
- **原型必须像产品**：悬停、键盘焦点、搜索快捷键、窄屏切栏、详情面板、宿主暗色适配和减少动态效果都属于设计交付，不是后续补丁。
- **在真实浏览器中验证**：布局需要通过本地 HTTP 页面，在目标尺寸和真实内容下检查，而不只依赖源代码推断。

## 视觉原则

1. **精致但不炫技**：采用中性炭黑、石墨灰、柔和银白和极少量琥珀，不使用 emoji、糖果色或大面积渐变。
2. **暗色但不发闷**：中性表面之间保持可见但克制的明度差；背景不得偏绿、偏蓝、偏粉、浑浊或把所有内容压成同一种黑灰。
3. **状态一眼可辨**：五栏各有一个克制的状态色和 Fluent 图标。颜色只用于导航、状态和反馈，不用于装饰。
4. **内容先于容器**：任务标题和下一步是卡片主角；项目、时间、归档和注意标签退到第二层。
5. **密度服务扫描**：在 Codex 约 824 × 752 的主工作区仍显示五栏，但正文不得小于可稳定阅读的尺寸，独立栏滚动。

## 字体

- 标题：`Segoe UI Variable Display`，回退到 `Segoe UI Variable Text` 与 `Microsoft YaHei UI`。
- 正文：`Segoe UI Variable Text`，中英文共用 Windows 本地字体栈，不加载远程字体。
- ID：`Cascadia Mono`，回退到 `Consolas`。
- 中文正文行高保持约 1.5；任务标题使用更高字重和紧凑但不拥挤的行高。

## 核心色彩

所有颜色都必须通过 `app.css` 的自定义属性使用，不在组件中散落新的十六进制颜色。

| 语义 | 色值 | 用途 |
| --- | --- | --- |
| 工作区 | `#181818` | 页面与栏间背景，贴合 Codex 暗色宿主 |
| 卡片 | `#242424` | 可交互任务表面 |
| 主文字 | `#e5e3df` | 正文与重要信息 |
| 次文字 | `#aaa6a0` | 时间、路径、辅助说明 |
| 交互色 | `#c9c3b8` | 焦点、已同步与选中 |
| 接棒色 | `#d9ad64` | 等待输入、审批与待验收 |

错误色只出现在真实错误上；它不能代替普通注意、空状态或运行状态。

## 状态栏语义

- **收集箱**：中性石墨灰，表示尚未归类，而不是失败。
- **已规划**：低饱和沙灰，表示方向已明确。
- **执行中**：暖象牙银，表示系统正在推进；允许一个缓慢呼吸的活动点。
- **待验收**：低饱和琥珀，表示轮到用户接棒，而不是告警。
- **完成**：仅在小面积状态元素中使用沉静石灰色，表示结果稳定、可回看。

状态色出现在栏图标、计数、轻量背景和必要的徽标中。任务卡不使用整条高饱和左边框，也不因状态变成大色块。

## 组件规则

### 顶部工作区

- 品牌标记是一个 2 × 2 任务网格，右下角的亮点代表正在推进的工作。
- 标题回答“这是什么”；摘要只显示项目数、任务数和归档数。
- 项目、搜索和归档组成一个工具组。搜索支持 `Ctrl/Cmd + K` 聚焦，`Esc` 清空。
- 同步状态使用柔和银灰小圆点；数据过期时改为琥珀色，并保留上一次成功结果。

### 等你接棒

- 原“需要我处理”改为“等你接棒”，强调这是自然的工作交接。
- 只显示由线程元数据确认的事项，不凭标题猜测。
- 没有事项时给出安静的完成式反馈，不显示空白警告框。

### 任务卡

- 顺序固定：标题 → 下一步 → 状态与元数据。
- “下一步”使用文字提示，不依靠只对设计师有意义的图标。
- 活动、注意、项目、归档和时间按重要性逐级降噪。
- 悬停只抬高 1px 并增强阴影；键盘焦点必须比悬停更明显。
- 本地栏位整理仍采用明确点击，不伪装成可拖放界面。

### 详情面板

- 从右侧进入，保持用户对原看板位置的记忆。
- “下一步”先于技术元数据；线程 ID 放在最后并支持选择复制。
- 点击遮罩、关闭按钮和原生 `Esc` 都能退出。

## 响应式与可访问性

- 700px 以上同时显示五栏；以下切换为状态标签和单栏视图。
- 静态、无脚本的 Codex 侧栏在窄屏改为纵向五栏，保证内容不丢失。
- 交互目标在窄屏至少 38px；重点操作应继续向 44px 靠拢。
- 所有输入都有可访问名称，状态更新使用 `aria-live`，标签切换使用 `aria-pressed`。
- 视觉固定为适配 Codex 的暗色方案，避免系统主题切换造成突兀的白色面板；继续支持 `prefers-reduced-motion`。
- 正文、次要文字和注意色需要达到 WCAG AA 对比度；不能只依靠颜色表达活动、注意或选中。

## 禁止项

- 不使用远程字体、远程图片、emoji 图标或运行时设计依赖。
- 不使用大面积渐变、玻璃拟态、霓虹光、彩色左边框模板或装饰性数据卡。
- 不把普通空状态写成错误，也不把归档或等待描述成失败。
- 不为了“看起来丰富”加入产品没有的数据、能力或安全承诺。
- 不修改 Projectboard 的只读安全边界来换取视觉或交互效果。
